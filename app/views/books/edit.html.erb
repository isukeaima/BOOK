<% if @book.errors.any? %>
<div id="error_explanation">

  <h2><%= @book.errors.count %>error rohibited this book from being saved:</h2>
    <ul>
      <li>
        <% @book.errors.full_messages.each do |message| %>
        <%= message %>
        <% end %>

      </li>
    </ul>
  
</div>
<% end %>

<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>

<h1>Editing Book</h1>
<%= form_with model: @book,local:true do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>
  <h4>Body</h4>
  <%= f.text_area :body %>
<%= f.submit 'Update Book' %>
  <% end %>
  
<span><%= link_to "Show", book_path  %></span>
|
<span><%= link_to "Back", books_path(@books) %></span>
</body>